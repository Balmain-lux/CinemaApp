<Window x:Class="CinemaApp.Pages.User.AddReviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaApp.Pages.User"
        mc:Ignorable="d"
       Title="Отзыв о фильме"
        Width="450" 
        Height="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource SecondaryBrush}"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Добавление отзыва" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0,0,0,20"/>

        <!-- Выбор фильма -->
        <TextBlock Grid.Row="1" Text="Фильм" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbMovies" 
                  Grid.Row="2"
                  Height="40"
                  DisplayMemberPath="Title"
                  SelectedValuePath="MovieID"
                  Margin="0,0,0,20"/>

        <!-- Рейтинг -->
        <TextBlock Grid.Row="3" Text="Оценка" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="star1" Content="☆" FontSize="30" Width="50" Click="star1_Click"  Tag="1"/>
            <Button x:Name="star2" Content="☆" FontSize="30" Width="50" Click="star1_Click" Tag="2"/>
            <Button x:Name="star3" Content="☆" FontSize="30" Width="50" Click="star1_Click"  Tag="3"/>
            <Button x:Name="star4" Content="☆" FontSize="30" Width="50" Click="star1_Click"  Tag="4"/>
            <Button x:Name="star5" Content="☆" FontSize="30" Width="50" Click="star1_Click"  Tag="5"/>
        </StackPanel>

        <!-- Комментарий -->
        <TextBlock Grid.Row="5" Text="Комментарий" Margin="0,0,0,5"/>
        <TextBox x:Name="txtComment" 
                 Grid.Row="6"
                 Style="{StaticResource CustomTextBox}"
                 Height="120"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,20"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Отмена" 
                    Style="{StaticResource SecondaryButton}"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="btnCancel_Click"/>
            <Button x:Name="btnSave" Content="Сохранить" 
                    Style="{StaticResource PrimaryButton}"
                    Width="150"
                    Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
