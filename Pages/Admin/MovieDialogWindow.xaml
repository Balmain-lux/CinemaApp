<Window x:Class="CinemaApp.Pages.Admin.MovieDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaApp.Pages.Admin"
        mc:Ignorable="d"
        Title="Фильм"
        Width="500" 
        Height="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource SecondaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название фильма" Margin="0,0,0,5"/>
        <TextBox x:Name="txtTitle" Style="{StaticResource CustomTextBox}" Grid.Row="1" Margin="0,0,0,15"/>

        <TextBlock Text="Описание" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="txtDescription" 
                Style="{StaticResource CustomTextBox}" 
                Grid.Row="3" 
                Height="80"
                TextWrapping="Wrap"
                AcceptsReturn="True"
                Margin="0,0,0,15"/>

        <Grid Grid.Row="4" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Длительность (мин)"/>
                <TextBox x:Name="txtDuration" Style="{StaticResource CustomTextBox}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                <TextBlock Text="Год выпуска"/>
                <TextBox x:Name="txtYear" Style="{StaticResource CustomTextBox}"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Возрастное ограничение"/>
                <TextBox x:Name="txtAge" Style="{StaticResource CustomTextBox}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="5" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Страна"/>
                <TextBox x:Name="txtCountry" Style="{StaticResource CustomTextBox}"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Режиссер"/>
                <TextBox x:Name="txtDirector" Style="{StaticResource CustomTextBox}"/>
            </StackPanel>
        </Grid>

        <TextBlock Text="Жанры" Grid.Row="6" Margin="0,0,0,5"/>
        <ListBox x:Name="lstGenres" 
                Grid.Row="7" 
                SelectionMode="Multiple"
                Height="120"
                Margin="0,0,0,15"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Отмена" 
                    Style="{StaticResource SecondaryButton}"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="btnCancel_Click"/>
            <Button x:Name="btnSave" Content="Сохранить" 
                    Style="{StaticResource PrimaryButton}"
                    Width="100"
                    Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
