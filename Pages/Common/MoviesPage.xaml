<Page x:Class="CinemaApp.Pages.Common.MoviesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CinemaApp.Pages.Common"
      mc:Ignorable="d" 
     Title="Фильмы"
      Background="{StaticResource SecondaryBrush}" >

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" 
                      Text="Наши фильмы" 
                      Style="{StaticResource PageTitle}"/>

            <!-- Фильтры по жанрам -->
            <WrapPanel Grid.Row="1" Margin="0,0,0,20">
                <Button x:Name="btnAllGenres" 
                        Content="Все" 
                        Width="80" Height="40"
                        Margin="0,0,10,10"
                        Style="{StaticResource PrimaryButton}"
                        Click="btnAllGenres_Click"/>
                <!-- Жанры будут добавлены динамически -->
                <ItemsControl x:Name="GenresPanel">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </WrapPanel>

            <!-- Сетка фильмов -->
            <WrapPanel x:Name="MoviesPanel" Grid.Row="2"/>
        </Grid>
    </ScrollViewer>
</Page>
